<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Register - Dog Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <style>
    body {
      background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('/images/background.jpg') no-repeat center center fixed;
      background-size: cover;
      font-family: 'Segoe UI', sans-serif;
      color: #333;
    }

    .register-box {
      background: rgba(255, 255, 255, 0.95);
      border-radius: 16px;
      padding: 2.5rem;
      max-width: 460px;
      margin: 10vh auto;
      box-shadow: 0 15px 30px rgba(0,0,0,0.2);
      transition: transform 0.3s ease;
    }

    .register-box:hover {
      transform: translateY(-5px);
    }

    .register-box h3 {
      font-weight: 700;
      color: #2d3436;
    }

    .form-control {
      padding-left: 2.5rem;
    }

    .form-label {
      font-weight: 600;
      color: #34495e;
    }

    .form-group {
      position: relative;
    }

    .input-icon {
      position: absolute;
      left: 15px;
      top: 10px;
      font-size: 1.2rem;
      color: #6c757d;
    }

    .btn-success {
      background: linear-gradient(90deg, #55efc4, #81ecec);
      border: none;
      font-weight: bold;
    }

    .btn-success:hover {
      background: linear-gradient(90deg, #00b894, #00cec9);
    }

    .text-center a {
      color: #0984e3;
      text-decoration: none;
    }

    .text-center a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div class="register-box text-center">
    <img src="/images/logo.png" alt="Dog Logo" style="height: 60px;" class="mb-3">
    <h3 class="mb-4">Create Your Dog Dashboard Account</h3>

    <form method="POST" action="/register">
      <div class="mb-3 form-group text-start">
        <label for="username" class="form-label">Username</label>
        <input type="text" class="form-control" name="username" required>
      </div>

      <div class="mb-3 form-group text-start">
        <label for="password" class="form-label">Password</label>
        <input type="password" class="form-control" name="password" required>
      </div>

      <button type="submit" class="btn btn-success w-100 mt-2">Register</button>
    </form>

    <div class="mt-3 text-center">
      <small>Already have an account? <a href="/login">Login here</a></small>
    </div>
  </div>

</body>
</html>
